{
  "name": "command-runner",
  "displayName": "Command Runner",
  "description": "Store and run custom command sequences step by step",
  "version": "1.0.0",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "commandRunner.addSequence",
        "title": "Command Runner: Add New Sequence"
      },
      {
        "command": "commandRunner.runSequence",
        "title": "Command Runner: Run Sequence"
      },
      {
        "command": "commandRunner.manageSequences",
        "title": "Command Runner: Manage Sequences"
      },
      {
        "command": "commandRunner.deleteSequence",
        "title": "Command Runner: Delete Sequence"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "commandRunner",
          "title": "Command Runner",
          "icon": "$(terminal)"
        }
      ]
    },
    "views": {
      "commandRunner": [
        {
          "id": "commandSequences",
          "name": "Command Sequences"
        }
      ]
    },
    "menus": {
      "view/item/context": [
        {
          "command": "commandRunner.runSequence",
          "when": "view == commandSequences",
          "group": "inline"
        },
        {
          "command": "commandRunner.deleteSequence",
          "when": "view == commandSequences"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "@types/vscode": "^1.85.0",
    "@typescript-eslint/eslint-plugin": "^6.13.0",
    "@typescript-eslint/parser": "^6.13.0",
    "eslint": "^8.54.0",
    "typescript": "^5.3.2"
  }
}
